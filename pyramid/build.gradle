plugins {
    id 'com.android.library'
    id 'com.chaquo.python'
}

android {
    namespace "com.fongmi.pyramid"
    compileSdk 30

    defaultConfig {
        minSdk 21
        targetSdk 30
    }
}

python {
    pip {
        install "requests"
    }
}
