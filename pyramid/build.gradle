plugins {
    id 'com.android.library'
}

android {
    namespace "com.fongmi.pyramid"
    compileSdk 30

    defaultConfig {
        minSdk 21
        targetSdk 30
    }
}

dependencies {
    // 没有 python{}，pyramid 只作为普通模块
}
